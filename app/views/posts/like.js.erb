$("#like_<%= @post.id %>").removeClass('glyphicon-heart-empty').addClass('glyphicon-heart');
$("#like_<%= @post.id %>").attr("href", '/posts/<%= @post.id %>/unlike');
$("#likes_<%= @post.id %>").html("<%= escape_javascript(render partial: 'posts/likes', locals: { post: @post} ) %>");
