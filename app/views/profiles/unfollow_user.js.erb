$('#unfollow-button').text('Follow')
                     .attr('class', 'btn follow-button')
                     .attr('href', "/<%= @user.user_name %>/follow_user")
                     .attr('id', 'follow-button');

// $("#unfollow-button").html("<%= escape_javascript render(partial: 'profiles/show') %>");
